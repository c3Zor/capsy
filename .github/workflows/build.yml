name: Build

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build (iOS Simulator, be pasirašymo)
        run: |
          xcodebuild build \
            -project Capsy.xcodeproj \
            -scheme Capsy \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            | tail -100
          exit ${PIPESTATUS[0]}

      # Sėkmingai subuildintas .xcodeproj įkomitinamas atgal — vartotojui
      # nereikia xcodegen: tiesiog clone → open Capsy.xcodeproj → Run.
      - name: Commit generated project
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f Capsy.xcodeproj
          git diff --cached --quiet || { git commit -m "CI: sugeneruotas Capsy.xcodeproj [skip ci]"; git push; }
